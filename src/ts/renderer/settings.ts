import App from "./app.js";

export default class Settings {
    private containerElement: HTMLElement | null = document.querySelector("settings-container");

    constructor(private app: App) {

    }

    public open(): void {

    }

    public close(): void {
        
    }
};
